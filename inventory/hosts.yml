# Replace <media-host-ip> and <network-host-ip> below with the actual
# IP address or hostname of each target machine before running the playbook.
media:
  hosts:
    media-host:
      ansible_host: <media-host-ip>

network:
  hosts:
    network-host:
      ansible_host: <network-host-ip>
